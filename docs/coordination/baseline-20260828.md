# Runcard browser baseline — RC-20260828-01

Tester: Codex. 2026-08-28, approximately 07:51–07:58 UTC.
Target: http://localhost:5173/ in Codex's in-app browser.
Source snapshot: HEAD 5547983, pre-existing index.html Google Fonts additions;
main.tsx imports index.css. No application source changed by Codex.

**Later status (08:26 UTC): batch paused.** RC-001 missing-run/comparison recovery
was fixed by Claude and independently verified in the browser. RC-002 now has
six named native stage buttons and visible focus styling; mouse behavior works,
but Enter/Space/Tab did not produce their expected effects through the browser
controls. Cause unresolved, so keyboard acceptance remains blocked, not passed.
Codex independently ran 585 passing tests and a successful build after the edits.
See codex.md for retest evidence and stop record. The original observations below
are retained as a historical baseline; no live deployment has been certified.

## What was actually exercised

| Boundary | Observation |
| --- | --- |
| Home | Renders a table of ten runs with production lengths, ligand, atom count, ΔG, and PLIP availability. |
| WebMCP discovery | Ten tools exposed by the actual browser capability. Registration and tool calls are not simulated by importing source functions. |
| list_runs | Returns ten records corresponding to the visible home table. |
| get_run_manifest | 1l2y-regression returns its manifest and navigates the page to that run. Call appears in the on-page log. |
| Structure | Screenshot shows a rendered protein cartoon and ligand; no WebGL fallback needed in this browser. |
| get_stage_input | product returns the archived production input with dt=0.002, nstlim=2500, ig=-1, restarting from density. |
| validate_stage | All six stages of 1l2y-regression return PASS under the current validator. This is not a scientific reliability certification. |
| explain_result | Returns uncertainty, drift, seeds, run spread, warning text, residual magnitudes, and provenance. Numerical methodology has not been independently audited. |
| get_ensemble | Returns all-run n=9 and >=10 ps n=5, including individual production lengths. |
| diff_runs | regression vs rep4 returns production 5 vs 30 ps and nstlim/ntwx differences; page opens the corresponding compare view. |
| propose_change | product dt=0.001 proposal pmtcnnsjx returned pending with before/after validation. No Approve or Reject click was made. |
| list_proposals | The same proposal was pending. |
| generate_rerun_bundle | pinned/local returns ten file names, README/run.sh, and applied_proposals=[]. Page shows the download control. |
| Actual ZIP download | Button produced a saved ZIP; inspected with Python zipfile without executing its scripts. CRC check returned no corrupt entry. |
| Console | No errors/warnings returned during the successful run/bundle path. |
| Missing-run tool call | Fails with a raw HTML-as-JSON parser message; reported as RC-001. |
| Missing-run URL | Still displays loading after its loader has failed; reported as RC-001. |

## Download evidence and test-harness caveat

Saved file: /Users/kevinzhou/Downloads/1l2y-regression-rerun-pinned-local.zip
Observed size: 8366 bytes. Modification time: 2026-08-28T07:54:04.815554Z.
SHA-256: 79a4276a3471dcf60b299f531f8bd71c5026875bbd485ad0ebf0b70ba9a4daf9

Archive entries: md/min1.in, md/min2.in, md/min3.in, md/heat.in, md/density.in,
md/product.in, build/leap.in, run.sh, README.md, manifest.json.
product.in contains dt=0.002 and ig=702337: the pending dt=0.001 edit was NOT
applied. README says approved changes: none and names the missing original build
inputs needed to rerun. No MD execution attempted.

The browser's download-event wait timed out (3 s, then 15 s) even though the
initial click saved the file. Browser bindings were also lost across an
interruption and restored to the same open run tab. Those are harness observations,
not evidence that the website's download is broken. A second download click may
have created a duplicate file; no download files were deleted.

## Presentation observations to revisit (not dispatched fixes)

- Green “all stages PASS” is prominent above the later MMPBSA warning and drift
  information. Consider whether a newcomer understands the validator's scope.
- The compare explanation says a longer run is “a better-converged sample.” This
  is stronger than merely reporting that length differs; needs a careful
  scientific wording review, not an automatic rule/statistics change.
- Stage controls appear as generic elements in the accessibility snapshot;
  keyboard accessibility is not yet tested.
- Tool calls show truncated raw JSON. Useful as an audit log, but not a readable
  replacement for the agent explanation.

## Not verified

Live Vercel URL/access, intended preprint theme, every run, mobile layout,
keyboard stage navigation, invalid comparisons, approved-proposal application,
fresh/SLURM download, reproduction of an actual MD calculation, or the scientific
validity/convergence of these runs. Reported test/build results from Claude are
not independent Codex checks. Broader testing pauses at RC-001 pending retest.
