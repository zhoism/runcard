# Runcard live batch 03 — new analysis and planning tools

Target: https://runcard.vercel.app/ (build 295bcb5,
`index-CrhCAa5N.js`). Browser: Codex built-in browser. Started
2026-08-29T04:24:10Z. Evidence comes from live WebMCP and visible page state.

## Passed before approval

- Header and discovery expose 12 tools.
- Rep4 discard 6 ps: frames 21–100, -19.35 ± 0.27, no drift; visible reanalysis
  line and shaded sparkline window.
- Regression discard 1 ps: -18.34 ± 0.56, N_eff=8.9, too short to judge;
  matching visible reanalysis and shading.
- interval=100 gives an actionable minimum-frame error.
- Regression plan: six more independent runs ≥10 ps and suggested nstlim=5000.
- 3HTB/JZ4 plan: honest single-run fallback; no ensemble uncertainty claim.
- list_proposals was [] before and after both plan calls. Planning is read-only.
- Tool Calls summaries state the values and conclusions without requiring JSON.

## Human checkpoint

Proposal pmtdvonl3 was created only by explicitly passing plan_sampling's
suggested edit to propose_change. It is pending with before/after PASS. The user
must approve it; only then can the pinned/local bundle be generated and verified.

The user approved it. The live bundle reports applied_proposals=[pmtdvonl3], ten
files, and a README recording product nstlim=5000. The final archive contents are
pending a manual download because the browser automation did not save the ZIP.

The user supplied the new ZIP. CRC and all ten entries pass; product parameters
correctly contain nstlim=5000, dt=0.002 and pinned ig=702337. However its first
line still says `5.0 ps`, contradicting the 10 ps parameters and README. This is
verified failure RC-003 round 1, dispatched rather than treated as presentation
feedback. All earlier new-tool checks remain passed.

RC-003 fix was deployed as d604351 / index-DoMhorJt.js. Live retest confirmed the
new asset and created fresh pending proposal pmtdwip7g from plan_sampling's exact
suggestion. A new human approval and bundle download are required because proposal
state from the previous deployment does not carry across reloads.

The user approved pmtdwip7g; Codex did not click Approve. Live WebMCP then returned
the proposal as approved and generated a ten-file bundle with
applied_proposals=[pmtdwip7g]. Fresh archive (4) passed CRC and entry checks.
`md/product.in` says `10.0 ps` and contains nstlim=5000, dt=0.002, and pinned
ig=702337; README records the 5 → 10 ps approved extension. SHA-256:
964f4f00ded5dc58419dfc5715296830c8f61def8656a94f1808f89a453c880d.
RC-003 is verified and batch 03 is complete after one fix/retest round.
