# Runcard live judge batch 04

Target: https://runcard.vercel.app/. User-reported revision 7d77378; actual
browser-served asset `/assets/index-B7PiDcQK.js`. Browser: Codex built-in browser
with real WebMCP. Started 2026-08-30T01:38:49Z.

## Passed

- Header and discovery: 14 registered tools.
- temp0 310 K controlled extension on 1l2y-rep4: density + product proposals,
  heat unchanged, readable jump-then-equilibrate note, two PASS→PASS proposals.
- Full test approval: pmtf58z581 density + pmtf58z582 product. Bundle tool result:
  13 files, self-contained, no missing build inputs, both proposals applied,
  Fork README with question/treatment/lineage and no partial warning.
- Isolated partial approval: pmtf5cgvo1 density approved, pmtf5cgvo2 product left
  pending. README warns `⚠ partially approved: product NOT changed`.
- stages=[heat] error names allowed stages density/product.
- Tool Console prefilled explain_result with 1l2y-rep4; Call succeeded.

## Verified failures dispatched as RC-004 round 1

- confidence_ladder and visible page card say 2 of 4 verified; independently
  replicated is only partly established because only two 30 ps runs exist. READY
  expected 3 of 4 and independent replication verified. Reconcile the scientific
  definition or deployment/acceptance mismatch; do not silently loosen it.
- fork_experiment replicate on single-run 3htb-jz4 never states the expected
  `no run-to-run estimate yet; at least 3 runs needed` guidance.

## Harness limitations

The live Download button was clicked, but the in-app browser backend produced no
download event or persisted file. ZIP-internal mdin and manifest fork.complete
checks remain pending an obtained archive; WebMCP return metadata/README were
verified. The flag-off header-pill path cannot be tested in the required flag-on
WebMCP browser session.

## RC-004 live retest

Live corrected asset `/assets/index-DwFbZHdi.js`:

- Confidence dispute accepted: 2/4 is intentional and scientifically narrower;
  one more 30 ps replicate is needed for the independent rung.
- 3htb-jz4 replicate guidance fixed and verified: minimum 3, two additional,
  explicit no-spread-yet explanation.
- Full fork regression passed again. Approved corrected-build proposals:
  pmtf5ut4h1 density and pmtf5ut4i2 product. Bundle metadata says 13 files,
  self-contained, both applied, heat unchanged.
- Download capture timed out again. Batch paused for the fresh archive rather
  than claiming ZIP internals from tool metadata.

Full archive (3) later passed: 13 entries/CRC clean; heat 300 K; density/product
310 K; self-contained build inputs; complete Fork README; manifest parent rep4
and fork.complete=true; SHA-256
6005ea20ea533abb0ac36f98690102527898b2acac30d1c39f3fd9eb0d8ae048.

Clean partial bundle prepared after forcing a genuinely new document. Approved
pmtfe6cqc1 density only; pmtfe6cqc2 product remains pending. README warning and
single applied ID pass via WebMCP; partial archive still needed for
manifest.fork.complete=false.

## Clean partial archive verification

User supplied `/Users/kevinzhou/Downloads/1l2y-rep4-rerun-fresh-local (4).zip`.
All 13 expected entries pass CRC; archive size is 13514 bytes and SHA-256 is
`e355d370f0423259e016b972f9bb7327bf9dd4ec021cb356671c0fa0d4c854a5`.
The inputs preserve heat at 300 K and product at 300 K while density is 310 K.
README explicitly warns that product was not changed and the bundle is not the
controlled extension as proposed. Manifest lineage names parent `1l2y-rep4`
and fork `fmtfe6cqb1`, with density applied, product not applied, and
`fork.complete=false`. Batch 04 is complete after one fix/retest round.
