# Runcard live judge batch 07

Target: https://runcard.vercel.app/. User-reported live revision 0c6fc66 and
bundle `/assets/index-Buc2vnBI.js`; actual browser asset must be recorded.
Browser: Codex built-in browser with real WebMCP. Started
2026-09-01T01:31:30Z; expires 2026-09-01T03:31:30Z.

Acceptance source: `READY — batch 07 request` in claude.md. Automode trace
variation and the no-proposal invariant are critical first checks. Round 0/3.

## Round 1 live baseline

Actual asset `/assets/index-Buc2vnBI.js`; 16 tools discovered, nine read-only.
Automode's three traces differ and create no proposal or bundle. Rep4 confidence
is 3/4 with the exact six-at-30-ps and mixed-engine disclosure. Global proposals,
full/partial bundles, custom pinned seed, session-aware evidence exports, console,
rendering, card order, and run-specific prompts otherwise pass.

RC-005 dispatches two verified failures: the `1l2y-rep4-ice1` rendered card does
not visibly name parent `1l2y-rep4`, and the evidence brief entropy sentence
prints literal `\\_MMPBSA\\_info` rather than clean `_MMPBSA_info`.

Approvals recorded: `pmthzwf5v1`, `pmthzwf5w2`, `pmthzz8vi1`, and
`pmthzzw6q1`. Product proposal `pmthzz8vj2` remained pending. Archives `(9)` and
`(10)` plus the custom pinned ZIP all passed CRC and exact input/manifest checks;
checksums are recorded in codex.md.

The 390 px item remains unverified because the live in-app WebMCP browser has no
viewport-resize control and no connected Chrome browser is available. This is a
harness limitation, not a site failure. Round 1/3 is ready_for_claude.

## RC-005 live retest

On served asset `/assets/index-BKr9BWEa.js`, lineage is now visible beneath the
ICE replicate title and in Provenance. Evidence briefs in both session modes have
no literal `\\_`; underscored identifiers are clean code spans. Session inclusion
and exclusion, automode's distinct/read-only traces, the empty proposal invariant,
and the 3-of-4 mixed-engine confidence evidence all remain correct. RC-005 is
verified after round 1.

Batch remains paused rather than complete solely because no available real-browser
surface can set a 390 px viewport. The mobile check is unverified, not failed.
