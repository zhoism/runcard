# Runcard live judge batch 08

Target: https://runcard.vercel.app/. User-reported revision 7aa4eb5 and bundle
`/assets/index-CzZf_syG.js`; actual browser asset must be recorded. Browser:
Codex built-in browser with real WebMCP. Started 2026-09-01T04:20:45Z; expires
2026-09-01T06:20:45Z. Round 1/3 dispatched as RC-006.

Acceptance source: `READY — batch 08 request` in claude.md. First gate is the
15-file MM-GBSA recipe and system-specific `:1-20` versus `:1-163` receptor masks.
No generated AMBER recipe will be executed. The 390 px measurement is out of scope.

## Live evidence — 2026-09-01T04:34:36Z

Actual served asset: `/assets/index-CzZf_syG.js`. Header and discovery agree on
16 tools; nine have `readOnlyHint:true`.

Priority 1 mostly passes. The downloaded CRC-clean archives are:

- `/Users/kevinzhou/Downloads/1l2y-rep4-rerun-pinned-local (1).zip` — 15 files,
  14101 bytes, SHA-256 `4cb5aba8bf3bd4eaa2eee268c2774d311c677243df6128e6a36cd2a1e5acd2c2`.
  `analysis/mmgbsa.in` has frames 1–500/interval 5, igb=5, saltcon=0.100;
  `run_analysis.sh` uses `-m ':1-20'` and `trajin $MD/product.nc`.
- `/Users/kevinzhou/Downloads/3htb-jz4-rerun-pinned-local.zip` — 15 files,
  39243 bytes, SHA-256 `731e0a3685ebb36a2015da6d5dac478b3e3da0e25081c4aa01e9a4a7cd604a47`.
  It uses `-m ':1-163'` / ligand `:164` and contains no `:1-20`.

Pinned README says it should reproduce -19.1953 on the same build and says
"Nothing here was executed by the page". Fresh README correctly expects a value
within run-to-run spread instead. The SLURM result fails one exact acceptance:
line 2 is `#SBATCH --job-name=1l2y-rep4`, missing `-mmgbsa` (RC-006A).

Automode returns distinct traces for rep4, regression, and 3HTB; bottlenecks are
repeatable, analysis-window robustness/drift, and independent replication. The
proposal queue is empty before and after. However, rep4 automode says MOL.mol2,
MOL.frcmod, and protein_clean.pdb must come from the original build directory,
contradicting the direct self-contained bundle and archive contents (RC-006B).

RC-005 retest passes: ICE child parent sentence + `derived from`; no parent
treatment on rep4; both evidence-brief modes have no backslash-underscore and do
have `` `_MMPBSA_info` ``. Confidence is 3/4 with the exact engine-mix sentence
and matched-length SD ±0.80 versus pooled ±0.64. Global queue survived in-app
Run -> Home -> Compare navigation. Full fork ZIP `(11).zip` is complete with
heat 300 K and density/product 310 K. Partial ZIP `(12).zip` is incomplete with
only density at 310 K and the warning. Custom-seed pinned ZIP `(2).zip` retains
product ig=424242 and says it is no longer a replay. Console prefill is exactly
`{"run_id":"1l2y-rep4"}`; Call and Auto/Manual toggles work. The four approvals
and their proposal IDs are recorded in codex.md.

## Round-1 retest and completion — 2026-09-01T04:47:28Z

Live served `/assets/index-XXvRWZIK.js` (Claude revision `502cd50`). RC-006B is
verified: automode now says the pinned bundle is self-contained from archived
inputs and leaves proposals empty. RC-006A was tester error, not a product bug:
the unsuffixed name belongs to `run.sh`; downloaded ZIP inspection shows
`run_analysis.sh` line 2 is exactly
`#SBATCH --job-name=1l2y-rep4-mmgbsa`. The SLURM archive is CRC-clean, 15 files,
14189 bytes, SHA-256
`6c08fe9b5dae2a29c40e0a9740bafb349e7b35a994d390a81cfec2e183d78935`.
Batch 08 completed after one round.
