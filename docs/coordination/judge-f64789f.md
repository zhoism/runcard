# Judge scorecard — build f64789f (design-critic lens; Codex scientist/WebMCP lens re-run on ff85e2f, see judge-ff85e2f.md)

build: f64789f · bundle: index-CqSj8sfl.js · 2026-08-29 (design lens; headless Chrome only, no real WebMCP client — tool count read from DOM: 14 options in the Tool console select, 3 marked ✎)
scores: webmcp 8/10 · execution 6/10 · impact 7/10 · creativity 8/10

top costs (ranked, ≤ 6):
- [P2] Same numbers stated three times on one page · #/run/1l2y-rep4 · DOM: `-19.20 ± 0.66` headline, then "± is the run-to-run SD over 9 independent runs… within-run SEM (0.2779) is not", then `<dt>run-to-run</dt> n=9: mean -18.01, SD 0.66`, `<dt>within run</dt> corrected SEM 0.2779`, then ladder rung 3 "mean -18.01, run-to-run SD ±0.66" (grep: `0.66` ×3, `0.2779` ×2, `-18.01` ×2, `-19.1953` ×3) · Fix: make the ladder rungs one-line verdicts that link down to the energy card; keep the numbers in one place.
- [P2] Confidence ladder and Fork cards read as paragraphs, not a structure · #/run/* · rep4-1440.png: five rungs each carry a 2–3-line 13 px grey paragraph; at 2560 (rep4-2560.png) main has no effective cap, rung text runs ~2100 px / ~250 characters per line, and the Analyses gallery becomes a 10-across strip · Fix: badge + label + one short clause per rung, `details` for the evidence; cap main at ~1100 px.
- [P2] Sections contradict each other on the short run · #/run/1l2y-rep2 · DOM: within-run row ends `<b>drifting</b>`; two cards lower, `<span class="badge pass">verified</span> <b>robust to reasonable analysis choices</b>` with "discard first 50 %: … 1.8 σ … (criterion ≤ 2)". A judge reads "drifting" and "verified robust" on the same screen · Fix: when the drift verdict is "drifting", the robustness rung cannot say verified — downgrade to "not established: drifting" or state the caveat in the rung line.
- [P2] Empty Contacts card leaves a dead half-column · #/run/1l2y-rep4, 1l2y-rep2 · DOM `<p class="dim">no PLIP profile for this run</p>` in a full grid2 card beside a ~550 px energy card (rep4-1440.png: card is ~60 px tall, rest of the column blank) · Fix: on runs without PLIP, drop the card and let the energy card span, or collapse to a one-line note.
- [P3] Fork copy is a garden-path sentence and the affordances are misaligned · #/run/* · DOM h2 dim: "reproduce and replicate change nothing and need no approval; extend changes one variable and waits for yours"; ladder crop: "build pinned bundle" sits inline after "is stable." while "plan a replicate" / "draft an extension" wrap to their own line; on 3htb-jz4 the replicate row says "an executed rerun joins the run-to-run spread, which is the uncertainty to quote" while the energy card above says "no run-to-run spread yet" · Fix: "Reproduce and replicate need no approval; extend changes one variable and waits for yours." Put buttons in a fixed third column; on single-run systems say "would start a run-to-run spread".
- [P3] ALL CAPS status labels, seven times · #/run/* Stages card · DOM `input checks PASS` + six `<span class="badge ">PASS</span>` (rule: sentence case; rule: don't repeat a fact) · Fix: one "input checks: pass" badge in the header, per-stage only when a stage differs.

Rules checked and clean: smallest font-size in live CSS is 13 px (`.small`, `.badge`, `.tag`, `.webmcp` all 13px); no `text-transform:uppercase` or letter-spacing in CSS; `.big` is 22 px = h1, no hero-number breakage; header is not sticky/fixed so z-index is moot; header pill is a real `<a href="#tool-console">` (scrolls, does not route).

First-screen questions (home-1440.png): "what is this" — answered by the lede in one sentence. "why WebMCP" — lede names `navigator.modelContext` and the pill/console explain the flag; good. "what do I do next" — only the table links; nothing points a judge at the signature run, and the first row is "1L2Y + MOL (indole)" / `1l2y-regression`, the least explicable id on the site. Compare view (cmp-1440.png): the primary is right (the interp box), but "System: identical prepared system" and "Stage parameters: no parameter differences" each get a full card for one green line, and `seeds A/B` are an unlabelled mono row — three cards for four lines.

needs a product decision (not a fix):
- The "expected" badge on the repeatable rung: by the invariant it is the honest word, but it reads as "expected to pass" beside "verified"; consider "not executed" or "archived, not run".
- The fork card's hard-coded "draft an extension (temp0 → 310 K)" on every run, including 3htb-jz4: a canned example or a chooser?
- Home: feature 1l2y-rep4 (or order by the demo path) instead of a flat table starting with the regression run.
- 2560-wide layout: cap the content column or let cards reflow into more columns — either is a decision about where the eye should rest.

What genuinely earned points: the page has one honest primary per run — the ΔG with the run-to-run ± and a one-sentence reason it is the uncertainty to quote — and the sidebar "Proposals: None yet… nothing is applied until you approve it here" makes the human-approves loop visible before any tool is called. The confidence ladder is the right idea and the right five rungs, with "to climb:" on each unverified one, and the compare view's interp line (|ΔΔG| vs √2·SD → 1.7σ, consistent with sampling noise) is the kind of sentence a computational scientist would actually want written for them. The cost is density, not honesty; every number I traced in the DOM appears with its source file or method.

Scratch: /private/tmp/claude-501/-Users-kevinzhou-Downloads-Single-Particle-runcard/fdf2255c-1256-4564-ab5f-5cdb7dc2414a/scratchpad/judge-f64789f-design/ (home/rep4/cmp/jz4/rep2 PNGs at 1440, home/rep4/cmp at 2560, DOM dumps, live.css).

## Disposition (Claude, ff85e2f)
- fixed in ff85e2f: numbers stated once (ladder rungs are one-line verdicts, evidence in a disclosure; headline note no longer repeats the SEM); robustness rung cannot be "verified" on a drifting / too-short run (rep2 now reads "not stationary (drifting)"); no dead Contacts column (card only when PLIP exists); `.app` capped at 1720 px; fork copy sentence-case, buttons in their own column, single-run wording on replicate; per-stage PASS badges only when a stage differs from the overall verdict.
- left for the user (product decisions): the "expected" wording on the repeatable rung; the canned "temp0 → 310 K" extension button on every run (vs a chooser); featuring 1l2y-rep4 / ordering the home table by the demo path; compare view's three cards for four lines.
