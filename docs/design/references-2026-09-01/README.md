# Reference sites from webmcp.devpost.com/resources (captured 2026-09-01, 1440 px headless)

Read next to `runcard-home-2026-09-01.png`, ours on the same day.

| file | what it is | what it does that we don't |
|---|---|---|
| `openai-wandernote.png` | OpenAI showcase: trip itinerary + map, 11 tools | Three columns with one job each (inputs · the document · the map). Eyebrow labels, serif display headings with an italic accent word, a hero image, `agent suggestion` tags on items, "Your agent is invited" card, `WebMCP · 11 tools` pill top right, "Your edits are protected" footer. |
| `openai-sunday-table.png` | OpenAI showcase: weekly meal plan, 12 tools | Same system: left nav with counts, calm centre grid with photos, right list with checkboxes, one green accent, "Plan with your agent" card bottom-left, "Your edits stay yours". |
| `openai-margin-editor.png` | OpenAI showcase: local notes with agent comments, 10 tools | Document-first: one column of text at 65 ch, a toolbar, comments in the margin, WebMCP dot bottom-left. Nothing competes with the writing. |
| `cloudflare-coffee-store.png` | Cloudflare demo | Six photos and one cart; every tool is a verb a person would also use ("Add"). WebMCP status is a footer dot. |
| `vercel-shop.png` | Vercel storefront | One centred display line, then the grid. Mono prices. Whitespace does the work. |
| `netlify-webmcp-starter.png` | Netlify starter | Dark, mono headline, tool chips `get_profile READ · leave_message WRITE`, an amber "no modelContext in this browser" notice with the flag. |
| `cloudflare-challenge-landing.png` | Cloudflare landing | Floating tool-call chips (`add_to_cart() awaiting confirmation`) as decoration around a display headline. |
| `openai-showcase-webmcp-apps.png` | the showcase gallery | The bar: nine apps, each an editorial layout with a real hero, not a report. |

Common to all of them: one display face with a serif or mono accent, one accent colour, eyebrow labels in small caps,
a `WebMCP · N tools` pill instead of a sentence, agent presence shown as tags on items and one "invite your agent" card,
whitespace instead of borders, and no paragraph before the first object on the page.
